import AppNavbar from '../../core/components/Navbar'

const HomePage = () => {
    return (
        <div>
            <AppNavbar/>
            <h1>Aqui estará el home </h1>
        </div>
    );
};

export default HomePage;
